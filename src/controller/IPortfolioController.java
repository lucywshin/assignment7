package controller;

/**
 * A controller which interacts with the {@code PortfolioModel} and {@code PortfolioView} to run the
 * application.
 */
public interface IPortfolioController {

  /**
   * A persistent method which runs the application.
   */
  void runApplication();
}
